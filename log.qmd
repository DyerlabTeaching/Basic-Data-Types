---
title: "Teaching Log"
format: html
---


## 2026.08.27
- BIO516: 
- ENVS543:  

## 2026.08.25
- BIOL516: Started working slides, got to [here](https://dyerlabteaching.github.io/Basic-Data-Types/slides.html#/data-classes)


### 2026.08.28 Population Genetics
-   8-28: Quick run through of the slides
-   9-2: Mostly did [In Class Activity](https://dyerlabteaching.github.io/Basic-Data-Types/in-class_data_types.html) No one finished but I'm going to continue to Data Containers next lecture.

### Environmental Data Literacy
-   8-28: Started slides up to [here](https://dyerlabteaching.github.io/Basic-Data-Types/slides.html#/named-functions).
-   9-2: Finished slides
-   9-4: Worked all day on [In Class Activity](https://dyerlabteaching.github.io/Basic-Data-Types/in-class_data_types.html).

## Copy-Edit Log — Basic-Data-Types

Tracks copy-editing passes over this module's `.qmd` documents. Clear-cut
typos/grammar/spelling errors are corrected directly in the source. Items
that need an author decision are left in place but wrapped in
`<mark>...</mark>` so they show up highlighted in the rendered HTML.

### narrative.qmd — 2026-08-15

#### Fixed directly (typos, spelling, grammar)

| Line | Issue | Fix |
|---|---|---|
| 14 | missing terminal period after "represented as `NA`" | added |
| 24 | "numerical represenations" | "numerical representations" |
| 77 | "symbos"; "the breath of operators" | "symbols"; "the breadth of operators" |
| 87, 89 | "the parenthesis"/"the parantheses" used inconsistently (singular/misspelled) for the same pair of characters; "betwen"; "varible"; "arguements" | standardized to "the parentheses" throughout; "between"; "variable"; "arguments" |
| 122 | "other contnet" | "other content" |
| 133, 145 | "the length of `dyer`" / "the variable `dyer`" — no variable named `dyer` exists in the code; the actual variable holding `"Dyer"` is `name` (defined a few lines above) | changed both references to `` `name` `` to match the actual code; also fixed "varibles" → "variables" and "concatinate" → "concatenate" |
| 171 | "smush them togethers" | "smush them together" |
| 197 | "if you intention is" | "if your intention is" |
| 370 | "based upone date" | "based upon date" |
| 372 | "These are several challenges" (should be existential "there") | "There are several challenges" |
| 376 | "The provence of Newfoundland" | "The province of Newfoundland" |
| 379 | "What they did is made the second" (pseudo-cleft needs "was" + bare verb) | "What they did was make the second"; also "unix epich" → "unix epoch" (misspelled once, correct elsewhere in the same paragraph) |
| 387 | "easiest says" / "specify the a date" (duplicate article) / "coerce it into a data object" (should be "date object", matching the section topic) | "easiest ways" / "specify a date" / "coerce it into a date object" |
| 402 | "make a the date" (duplicate article) | "make a date" |
| 459 | "a format string were we indicate" / "for`?strptime`" (missing space) | "where we indicate" / "for `?strptime`" |
| 547 | "(paste being defined" — should be "past"; also sentence-initial lowercase | "Finding items that are in the past (past being defined...)" |
| 561 | "arithmatic operations" | "arithmetic operations" |

#### Flagged for your review (highlighted with `<mark>` in the source)

| Line | Text | Why it's flagged |
|---|---|---|
| ~77 | "The authors of `R` have decided that using combinations of symbols to handle these and you will get used to them in not time at all." | Doesn't parse as written ("have decided that using... to handle" is missing a verb, and "in not time" should probably be "in no time") — needs a real rewrite, not a word swap. |
| ~374 | "we use the Julian calendar" | The US (and most of the world) uses the **Gregorian** calendar, not the Julian calendar — the Julian calendar was replaced by the Gregorian in 1582 and is now mostly used only in some Orthodox liturgical contexts. This looks like a factual error, but since it's a content claim (not a typo) I left it for you to confirm/fix. |
| ~376 | "GMT-2:30" (Newfoundland's offset) | Newfoundland Standard Time is UTC−3:30, not −2:30, as far as I can tell. Flagged rather than silently changed since it's a specific factual claim. |
| ~402 | "you need to specify what elements within the string representation using format codes." | Missing a verb — "elements within the string representation" needs something like "represent" or "correspond to" before "using format codes." I didn't want to guess which. |
| ~541 | `` `weekday()` `` | Neither base R nor lubridate has a function called `weekday()` — the real functions are `wday()` (lubridate, used correctly earlier in this same document) and `weekdays()` (base R, also used correctly later). This looks like a conflation of the two; flagged rather than guessing which one you meant here. |
| ~319 | "Coercion of something else to a `Logical` is more case-specific." | This exact sentence appears twice — once here, under the **`### Introspection`** heading (followed by three character-to-logical coercion examples, `as.logical("TRUE")` etc., which are about coercion, not introspection), and again correctly under the real **`### Coercion`** heading a few lines later with the numeric-to-logical examples. It looks like the "Introspection" section accidentally absorbed content that belongs in "Coercion." I flagged the duplicate sentence rather than moving the code blocks myself, since reorganizing section content is a bigger structural change than a copy-edit. |
